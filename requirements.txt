# Núcleo (parsing FIT + datos + base analítica)
fitdecode
pandas
pyarrow
pyyaml
python-dotenv
duckdb

# Dashboard
streamlit
plotly

# Tests
pytest

# --- Fase 2 ---
# garminconnect     # descarga automática desde Garmin Connect (API no oficial)
